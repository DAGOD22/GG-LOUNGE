'use client'
import { useState } from 'react'
export default function RequestGame(){const [title,setTitle]=useState('');const [icon,setIcon]=useState('');const [html,setHtml]=useState('');const [message,setMessage]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/game-requests',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({title,icon,html})});setMessage(r.ok?'Request submitted for admin review.':'Upload a valid index.html file.')}return <main className="admin-shell"><section className="admin-panel request-form"><p className="eyebrow">GG-LOUNGE™ / COMMUNITY</p><h1>Request a game.</h1><p>Submit a self-contained index.html for review. Approved games are published permanently.</p><form onSubmit={submit}><label htmlFor="title">Game title</label><input id="title" value={title} onChange={e=>setTitle(e.target.value)} required/><label htmlFor="icon">Favicon URL (optional)</label><input id="icon" value={icon} onChange={e=>setIcon(e.target.value)} placeholder="https://..."/><label htmlFor="html">index.html file</label><input id="html" type="file" accept=".html,text/html" required onChange={e=>{const file=e.target.files?.[0];if(file){const reader=new FileReader();reader.onload=()=>setHtml(String(reader.result));reader.readAsText(file)}}}/><button type="submit">Submit for review</button></form>{message&&<p className="muted">{message}</p>}<a href="/">Back to lounge</a></section></main>}
